import { useEffect } from 'react';
import AOS from 'aos';
import { initPage } from './main'; // adjust path

export default function PageInit() {
  useEffect(() => {
    AOS.init({ duration: 800, easing: 'slide' });
    initPage();
  }, []);

  return null;
}